# Smoke Test

1. Create venv and install requirements.
2. Run: `python -m src.main --config config/config.yaml`
3. Confirm log lines show each task starting.
4. (Optional) Inject events in `main.py` and observe outputs.
